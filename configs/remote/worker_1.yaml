worker_id: 1
listen_address: "0.0.0.0:50051"

peers:
  - id: 0
    address: "10.96.0.255:50051"
  - id: 2
    address: "10.96.0.2:50051"

data:
  partition_id: 1
  dataset: "MNIST"
  non_iid_level: 0.5

training:
  batch_size: 32
  learning_rate: 0.01
  local_epochs: 1
  max_iterations: 100

mpls:
  tau1: 0.5
  tau2: 0.5

model:
  name: "SimpleMLP"

system:
  compute_speed: 1.0
  device: "cpu"

logging:
  level: "INFO"
  dir: "logs"
